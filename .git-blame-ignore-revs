# Revisions to ignore when running `git blame`.
# Configure locally with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs

# style: bulk-apply ruff-format + EOF + trailing-whitespace baseline (pre-commit)
de87406540c2e9f2529337314676477d3e0704fb
